# LanGraph (legacy snapshot)

This folder is an **older copy** of the project kept for reference. It may not include the latest features (multi-agent graph, web UI, workspaces, document library, etc.).

**Use the repository root instead:**

```bash
cd ..
pip install -e .
uvicorn knowledge_transfer_agent.api.main:app --reload
```

See the main [README.md](../README.md) for full setup and documentation.
